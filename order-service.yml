user:
  count: 940
  test: 950
server:
  port: 8200
spring:
  data:
    mongodb:
      uri: mongodb://${ORDER_DB_HOST:root}:${MONGO_PASS:mongopass}@${ORDER_DB_HOST:localhost}:27017/orderdb
product:
  service:
    url: http://${PRODUCT_SERVICE_NAME:localhost}:8300
#eureka:  
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    prefer-ip-address: true
#    ip-address: 127.0.0.1